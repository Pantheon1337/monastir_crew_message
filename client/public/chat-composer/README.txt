Иконки панели ввода чата (PNG с прозрачностью, ~24–32 px по большей стороне)

Положите файлы с ТОЧНЫМИ именами:

  icon-attach.png    — скрепка / вложение
  icon-stickers.png  — стикеры и эмодзи (кнопка открытия панели)
  icon-video.png     — режим видеокружка (когда выбран кружок)
  icon-mic.png       — режим голоса / микрофон

После добавления файлов пересоберите клиент (vite копирует public/ как есть).
Если файла нет — в интерфейсе останутся запасные символы (эмодзи).
